// import { Navigate } from "react-router-dom";
// import { Route } from "react-router-dom";

// const AdminRoute = ({ children }) => {
//   return isAuthenticated()?.role === 1 ? (
//     children
//   ) : (
//     <Navigate to="admin/template" replace />
//   );
// };
